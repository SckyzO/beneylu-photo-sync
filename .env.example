# .env.example
ENT_LOGIN=
ENT_PASSWORD=
ENT_DATA_DIR=./data
# ENT_BASE_URL=https://www.ent-ecole.fr
# ENT_STATE_DB=./state.db
# ENT_EXCLUDED_BOARDS=APEIT, Vie de l'école   # tableaux à ne pas synchroniser
# ENT_SYNC_WORKERS=4                          # téléchargements en parallèle

# --- Web UI (optional) ---
# ENT_WEB_PASSWORD=change-me        # protège l'accès à l'UI (sinon accès libre)
# ENT_SYNC_INTERVAL_HOURS=12        # sync auto toutes les N heures (0 = manuel)
